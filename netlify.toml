[build]
  command = "npm run build"
  publish = ".next"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Daily scheduler at 6:00 PM IST (12:30 PM UTC)
[functions."scheduler"]
  schedule = "30 12 * * *"